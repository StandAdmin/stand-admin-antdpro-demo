// import React from 'react';
import { PageContainer } from '@ant-design/pro-layout';

export default (props: any) => {
  return <PageContainer content={<div id="page-header-bottom"></div>} {...props} />;
};
